---
description: AI 接口稳定性规则（校验、超时、错误映射）
globs: app/api/**/*.{ts,tsx}
alwaysApply: false
---

# AI 接口规则

- API 入参必须使用 `zod` + `@hono/zod-validator` 校验。
- 第三方 AI 调用前检查关键环境变量，缺失时显式返回 `{ error }`。
- 异步任务必须定义轮询间隔、最大次数和超时错误。
- 返回结构统一：成功 `{ data }`，失败 `{ error }`。
- 记录关键失败日志：任务创建失败、轮询失败、结果缺失。
